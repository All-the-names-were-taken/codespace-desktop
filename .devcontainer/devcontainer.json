{
  "build": {
    "dockerfile": "Dockerfile"
  },
  "privileged": true,
  "forwardPorts": [6080],
  "portsAttributes": {
    "6080": {
      "label": "Desktop",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "mkdir -p ~/.vnc && printf 'password\n' | vncpasswd -f > ~/.vnc/passwd && chmod 600 ~/.vnc/passwd && git clone https://github.com/novnc/noVNC.git && bash .devcontainer/xfce-theme-setup.sh",
  "postStartCommand": "vncserver -xstartup \"/usr/bin/startxfce4 &\" -rfbport 5900 && ./noVNC/utils/novnc_proxy --vnc 0.0.0.0:5900 --listen 0.0.0.0:6080"
}
